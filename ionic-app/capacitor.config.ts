import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.picture',
  appName: 'Picture Picker',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
